// Legacy filename — kept for any deep imports. Real implementation lives
// in `src/Shuffle-Core/views/monitors/MonitorsView.tsx`. Prefer importing
// `MonitorsPage` from `./MonitorsPage` (or `MonitorsView` directly).
export { default } from './MonitorsPage';
